# OS / editor junk
.DS_Store
**/.DS_Store

# Claude Code local data (not part of the site)
.claude/

# Unused full-size original backup — the live site uses the optimized assets/photo.jpg
assets/photo-original.jpg

# Source video clips (large originals) — the optimized .mp4s in the photo folders ARE committed
*.mov
*.MOV

# Personal scratch notes (not part of the site)
notes to add.md
